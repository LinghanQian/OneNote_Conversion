[project]
name = "onenote-conversion"
version = "0.1.0"
description = "Convert OneNote export files to Markdown."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mammoth>=1.12.0",
    "markdownify>=1.2.2",
]

[tool.setuptools]
py-modules = ["onenote2md", "main"]
